# Python
__pycache__/
*.py[cod]

# Secrets — env.example is the tracked template
.env

# Captured/operational data, not source
sigmon_fallback.jsonl
*.iq
*.cfile

# Generated by --save: per-run calibration output, and rejected attempts.
# Regenerated by dfcal.py; a stale one that gets loaded is worse than none.
dfcal.json
dfcal.*.json
stepper.json
# ...except the accepted calibration, force-added:
!dfcal.LTE1800-h1.json
!dfcal.1810.json
